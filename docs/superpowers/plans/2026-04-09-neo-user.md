# Neo User Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Create non-root user `neo` in fullstack.Dockerfile for Claude Code bypassPermissions mode.

**Architecture:** Add two new sections to Dockerfile after Oh My Zsh configuration: (1) create neo user with zsh plugins, (2) switch USER and WORKDIR.

**Tech Stack:** Dockerfile, Ubuntu 22.04 base, Oh My Zsh, zsh plugins

---

## Files Structure

| File | Action | Responsibility |
|------|--------|----------------|
| `fullstack-image/fullstack.Dockerfile` | Modify | Add neo user creation and USER directive |

---

### Task 1: Add Neo User Creation Section

**Files:**
- Modify: `fullstack-image/fullstack.Dockerfile` (after line 112, before ENV SHELL)

- [ ] **Step 1: Add neo user creation section**

在第 8 节（Zsh 配置，约 line 112 `echo "alias vi='nvim'" >> ~/.zshrc` 之后）添加新节：

```dockerfile
# ==========================================
# 9. 创建 neo 用户（非 root）
# ==========================================
RUN useradd -m -s /bin/zsh neo && \
    # 为 neo 安装 Oh My Zsh (使用 --unattended 非交互模式)
    su - neo -c 'sh -c "$(curl -fsSL https://raw.githubusercontent.com/ohmyzsh/ohmyzsh/master/tools/install.sh)" "" --unattended' && \
    # 安装 zsh 插件 (root 执行，需 chown)
    git clone https://github.com/zsh-users/zsh-autosuggestions /home/neo/.oh-my-zsh/custom/plugins/zsh-autosuggestions && \
    git clone https://github.com/zsh-users/zsh-syntax-highlighting /home/neo/.oh-my-zsh/custom/plugins/zsh-syntax-highlighting && \
    chown -R neo:neo /home/neo/.oh-my-zsh/custom/plugins && \
    # 配置 .zshrc
    sed -i 's/plugins=(git)/plugins=(git zsh-autosuggestions zsh-syntax-highlighting)/' /home/neo/.zshrc && \
    sed -i 's/ZSH_THEME="robbyrussell"/ZSH_THEME="ys"/' /home/neo/.zshrc && \
    echo "alias vim='nvim'" >> /home/neo/.zshrc && \
    echo "alias vi='nvim'" >> /home/neo/.zshrc

# ==========================================
# 10. 切换到 neo 用户
# ==========================================
USER neo
RUN mkdir -p /home/neo/work
WORKDIR /home/neo/work
```

**Expected:** 新增约 20 行代码，位于原 `ENV SHELL=/bin/zsh` 之前。

---

### Task 2: Update CMD Directive

**Files:**
- Modify: `fullstack-image/fullstack.Dockerfile` (last 2 lines)

- [ ] **Step 1: Ensure CMD remains at end**

原文件末尾：
```dockerfile
ENV SHELL=/bin/zsh
WORKDIR /app
CMD ["sleep", "infinity"]
```

改为：
```dockerfile
ENV SHELL=/bin/zsh
CMD ["sleep", "infinity"]
```

注意：删除 `WORKDIR /app`，因为 neo 用户的工作目录已在 Task 1 设置为 `/home/neo/work`。

---

### Task 3: Commit Changes

- [ ] **Step 1: Review diff**

Run: `git diff fullstack-image/fullstack.Dockerfile`
Expected: 显示新增的 neo 用户创建代码

- [ ] **Step 2: Commit**

```bash
git add fullstack-image/fullstack.Dockerfile docs/superpowers/specs/2026-04-09-neo-user-design.md
git commit -m "feat: add neo non-root user for Claude Code bypassPermissions mode

- Create neo user with zsh shell
- Configure Oh My Zsh + plugins for neo
- Set work directory to /home/neo/work
- Switch default user to neo

Co-Authored-By: Claude Opus 4.6 <noreply@anthropic.com>"
```

---

### Task 4: Verification (Optional - No Local Testing)

根据 CLAUDE.md 项目要求：**不进行本地测试**，镜像通过 GitHub Actions CI/CD 构建推送。

- [ ] **Step 1: Push to trigger CI**

```bash
git push origin main
```

Expected: GitHub Actions 自动触发构建（因 `fullstack-image/**` 路径变更）

- [ ] **Step 2: Verify CI build success**

检查 GitHub Actions workflow 是否成功构建镜像。

---

## Self-Review Checklist

- [x] Spec coverage: All requirements from design spec covered
- [x] No placeholders: All code blocks contain actual Dockerfile code
- [x] Type consistency: No functions/types to verify (Dockerfile only)
- [x] File paths: Exact path `fullstack-image/fullstack.Dockerfile`
- [x] CI note: Added per project requirement (no local testing)